---
description: Sync versions across version.json, SKILL.md, and CHANGELOG.md on commits
globs: version.json, skill/SKILL.md, CHANGELOG.md
alwaysApply: false
---

# Version & Changelog Sync Rule

## Version Sync

When `version.json`'s `version` field is changed:
1. Update `skill/SKILL.md` frontmatter `version` to the same value
2. Search `skill/SKILL.md` body for any hardcoded version strings (e.g. `` `1.x.x` ``) and update them
3. **Never hardcode version numbers in body text** — if a reference to "current version" exists, it should say "Read the `version` field from the frontmatter" without repeating the actual number

## Auto-Update CHANGELOG on Commit

When creating a commit, add the changes to `CHANGELOG.md`.

- If the version was bumped: add a new version section (`## [x.y.z] - YYYY-MM-DD`)
- If the version is unchanged: append entries to the current top-level version section

```markdown
# Example CHANGELOG.md

## [1.1.0] - 2026-03-05
- Added new feature X
- Fixed bug in module Y

## [1.0.1] - 2026-03-02
- Version check system test
```
